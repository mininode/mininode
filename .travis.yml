language: c
matrix:
  include:
    - os: linux
      dist: precise
      sudo: false
    - os: osx
      osx_image: xcode8.2
addons:
  apt:
    packages:
      - graphviz  
script: git submodule init && git submodule update && make && make install
compiler:
  - clang
  - gcc
